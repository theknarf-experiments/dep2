#!/usr/bin/env bash
#MISE description="Live graph UI for examples/rust_panic_propagation.dl (arg: folder to analyze; wraps the graph task)"
set -euo pipefail
export GRAPH_PROGRAM="examples/rust_panic_propagation.dl"
exec "$(dirname "$0")/graph" "$@"
