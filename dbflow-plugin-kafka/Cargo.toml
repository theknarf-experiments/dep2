[package]
name = "dbflow-plugin-kafka"
version = "0.1.0"
edition = "2021"

[dependencies]
dbflow-plugin = { path = "../dbflow-plugin" }
rdkafka = { version = "0.36", features = ["cmake-build"] }
